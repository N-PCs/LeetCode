class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int result=0;
        for(int num:nums){
            result^=num;     //utilises XOR operator to find unique element
        }
        return result;
    }
};
