class Solution {
public:
    bool isPalindrome(int x) {
        string y=to_string(x);
        string reverse=y;
        std::reverse(reverse.begin(),reverse.end());
        return y==reverse;
    }
      
};
